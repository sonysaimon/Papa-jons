<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Premium Pizza Delivery | Order Online</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;800&family=Poppins:wght@300;500;700&display=swap" rel="stylesheet">
    <style>
        /* --- CSS VARIABLES & GLOBAL STYLES --- */
        :root {
            --red: #D7263D;
            --red-hover: #BF1E2E;
            --green: #2B9348;
            --green-hover: #1A7431;
            --beige: #F8F1E5;
            --white: #FFFFFF;
            --dark: #1F1F1F;
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            --radius: 16px;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--beige);
            color: var(--dark);
            overflow-x: hidden;
            line-height: 1.6;
        }

        h1, h2, h3, h4 {
            font-family: 'Montserrat', sans-serif;
            font-weight: 800;
        }

        a { text-decoration: none; color: inherit; }
        ul { list-style: none; }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            z-index: 10;
        }

        .btn {
            display: inline-block;
            padding: 14px 28px;
            border-radius: 50px;
            font-weight: 700;
            cursor: pointer;
            text-align: center;
            transition: var(--transition);
            border: none;
            font-size: 1rem;
        }

        .btn-primary { background: var(--red); color: var(--white); box-shadow: 0 4px 15px rgba(215, 38, 61, 0.4); }
        .btn-primary:hover { background: var(--red-hover); transform: translateY(-3px); }
        .btn-secondary { background: var(--green); color: var(--white); box-shadow: 0 4px 15px rgba(43, 147, 72, 0.4); }
        .btn-secondary:hover { background: var(--green-hover); transform: translateY(-3px); }
        .btn-outline { background: transparent; border: 2px solid var(--red); color: var(--red); }
        .btn-outline:hover { background: var(--red); color: var(--white); }

        /* --- PARALLAX BACKGROUND --- */
        #parallax-bg {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            pointer-events: none;
            z-index: 1;
            overflow: hidden;
        }

        .float-el {
            position: absolute;
            opacity: 0.6;
            background-size: contain;
            background-repeat: no-repeat;
        }
        
        .tomato { width: 80px; height: 80px; background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="45" fill="%23D7263D"/><circle cx="50" cy="50" r="35" fill="%23BF1E2E"/></svg>'); }
        .leaf { width: 60px; height: 60px; background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><path d="M10,90 Q90,90 90,10 Q10,10 10,90" fill="%232B9348"/></svg>'); }
        .cheese { width: 100px; height: 100px; background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><path d="M20,20 Q40,0 60,20 T90,50 Q80,80 50,80 T20,20" fill="%23FFD166"/></svg>'); }

        /* --- SCROLL ANIMATIONS --- */
        .fade-in {
            opacity: 0;
            transform: translateY(40px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }
        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- HEADER --- */
        header {
            position: fixed;
            top: 0; width: 100%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 15px rgba(0,0,0,0.05);
            z-index: 100;
            padding: 15px 0;
            transition: var(--transition);
        }

        .header-content { display: flex; justify-content: space-between; align-items: center; }
        .logo { font-family: 'Montserrat'; font-size: 1.8rem; font-weight: 800; color: var(--red); }
        .nav-links { display: flex; gap: 25px; }
        .nav-links a { font-weight: 600; color: var(--dark); transition: var(--transition); }
        .nav-links a:hover { color: var(--red); }
        .header-actions { display: flex; align-items: center; gap: 15px; }
        
        /* --- HERO SECTION --- */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            padding-top: 80px;
            position: relative;
            z-index: 10;
        }
        .hero-content { display: grid; grid-template-columns: 1fr 1fr; gap: 50px; align-items: center; }
        .hero-text h1 { font-size: 4rem; line-height: 1.1; margin-bottom: 20px; color: var(--dark); }
        .hero-text h1 span { color: var(--red); }
        .hero-text p { font-size: 1.2rem; margin-bottom: 30px; color: #555; }
        .hero-btns { display: flex; gap: 15px; }
        .hero-image img { width: 100%; max-width: 600px; animation: floatPizza 6s ease-in-out infinite; filter: drop-shadow(0 20px 30px rgba(0,0,0,0.2)); }

        @keyframes floatPizza {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(3deg); }
        }

        /* --- SECTIONS COMMON --- */
        section { padding: 100px 0; position: relative; z-index: 10; }
        .section-title { text-align: center; font-size: 2.5rem; margin-bottom: 50px; }

        /* --- DEALS SECTION --- */
        .deals-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 30px; }
        .deal-card {
            background: var(--white);
            border-radius: var(--radius);
            padding: 20px;
            box-shadow: var(--shadow);
            text-align: center;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }
        .deal-card:hover { transform: translateY(-10px); }
        .deal-card img { width: 100%; height: 200px; object-fit: cover; border-radius: 12px; margin-bottom: 15px; }
        .deal-price { font-size: 1.5rem; font-weight: 800; color: var(--red); margin: 10px 0; }
        .badge { position: absolute; top: 10px; right: 10px; background: var(--green); color: white; padding: 5px 12px; border-radius: 20px; font-weight: 600; font-size: 0.8rem; }

        /* --- MENU PREVIEW --- */
        .menu-cats { display: flex; justify-content: center; gap: 20px; margin-bottom: 40px; flex-wrap: wrap; }
        .menu-cat { padding: 10px 25px; background: var(--white); border-radius: 30px; font-weight: 600; cursor: pointer; box-shadow: var(--shadow); transition: var(--transition); }
        .menu-cat.active, .menu-cat:hover { background: var(--red); color: var(--white); }
        
        .menu-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 30px; }
        .menu-item { background: var(--white); border-radius: var(--radius); padding: 20px; text-align: center; box-shadow: var(--shadow); }
        .menu-item img { width: 150px; height: 150px; border-radius: 50%; object-fit: cover; margin: 0 auto 15px; transition: var(--transition); }
        .menu-item:hover img { transform: scale(1.1) rotate(5deg); }

        /* --- BUILD PIZZA --- */
        .build-section { background: var(--white); border-radius: 24px; padding: 50px; display: grid; grid-template-columns: 1fr 1fr; gap: 50px; align-items: center; box-shadow: var(--shadow); }
        .build-options h3 { margin-bottom: 15px; border-bottom: 2px solid var(--beige); padding-bottom: 5px; }
        .options-group { display: flex; gap: 10px; margin-bottom: 20px; flex-wrap: wrap; }
        .opt-btn { padding: 8px 16px; border: 1px solid #ddd; border-radius: 20px; cursor: pointer; transition: var(--transition); }
        .opt-btn:hover, .opt-btn.selected { background: var(--green); color: white; border-color: var(--green); }
        .build-preview { text-align: center; }
        .build-preview img { width: 100%; max-width: 400px; margin-bottom: 20px; }

        /* --- WHY CHOOSE US --- */
        .benefits-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 30px; text-align: center; }
        .benefit-card { padding: 30px; background: var(--white); border-radius: var(--radius); box-shadow: var(--shadow); }
        .benefit-icon { font-size: 3rem; margin-bottom: 15px; }

        /* --- REWARDS --- */
        .rewards-section { background: linear-gradient(135deg, var(--red), #901625); color: var(--white); border-radius: 24px; padding: 60px; text-align: center; position: relative; overflow: hidden; }
        .rewards-section h2 { font-size: 3rem; margin-bottom: 15px; }
        .rewards-section p { font-size: 1.2rem; margin-bottom: 30px; }
        
        /* --- REVIEWS --- */
        .reviews-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; }
        .review-card { background: var(--white); padding: 30px; border-radius: var(--radius); box-shadow: var(--shadow); }
        .stars { color: #FFD166; font-size: 1.2rem; margin-bottom: 10px; }

        /* --- STORE LOCATOR --- */
        .locator-container { background: var(--white); padding: 40px; border-radius: var(--radius); box-shadow: var(--shadow); display: flex; flex-direction: column; gap: 20px; align-items: center; }
        .search-bar { display: flex; width: 100%; max-width: 600px; }
        .search-bar input { flex: 1; padding: 15px; border: 2px solid var(--beige); border-radius: 30px 0 0 30px; outline: none; font-size: 1rem; }
        .search-bar button { border-radius: 0 30px 30px 0; }

        /* --- APP PROMO --- */
        .app-promo { display: grid; grid-template-columns: 1fr 1fr; gap: 50px; align-items: center; }
        .app-text h2 { font-size: 2.5rem; margin-bottom: 20px; }
        .app-text ul { margin-bottom: 30px; }
        .app-text li { margin-bottom: 10px; font-size: 1.1rem; }
        .app-text li::before { content: '✓'; color: var(--green); font-weight: bold; margin-right: 10px; }
        .app-mockup { width: 100%; max-width: 350px; margin: 0 auto; display: block; border-radius: 30px; box-shadow: 0 20px 40px rgba(0,0,0,0.2); }

        /* --- FINAL CTA --- */
        .final-cta { text-align: center; padding: 80px 0; }
        .final-cta h2 { font-size: 3.5rem; margin-bottom: 15px; }

        /* --- FOOTER --- */
        footer { background: var(--dark); color: var(--white); padding: 60px 0 20px; }
        .footer-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 40px; margin-bottom: 40px; }
        .footer-col h4 { margin-bottom: 20px; color: var(--red); }
        .footer-col ul li { margin-bottom: 10px; }
        .footer-col ul li a:hover { color: var(--green); }
        .footer-bottom { text-align: center; border-top: 1px solid #444; padding-top: 20px; font-size: 0.9rem; color: #888; }

        /* --- RESPONSIVE --- */
        @media (max-width: 900px) {
            .hero-content, .build-section, .app-promo { grid-template-columns: 1fr; text-align: center; }
            .hero-text h1 { font-size: 3rem; }
            .hero-btns, .options-group { justify-content: center; }
            .nav-links { display: none; /* In production, add a hamburger menu */ }
        }
    </style>
</head>
<body>

    <!-- PARALLAX BACKGROUND ELEMENTS -->
    <div id="parallax-bg">
        <!-- Javascript will populate these elements -->
    </div>

    <!-- HEADER -->
    <header id="header">
        <div class="container header-content">
            <a href="#" class="logo">Slice<span style="color:var(--green)">It</span></a>
            <nav class="nav-links">
                <a href="#deals">Deals</a>
                <a href="#menu">Menu</a>
                <a href="#rewards">Rewards</a>
                <a href="#locator">Locations</a>
            </nav>
            <div class="header-actions">
                <a href="#build" class="btn btn-primary">Order Now</a>
                <div class="cart-icon" style="font-size: 1.5rem; cursor:pointer;">🛒</div>
            </div>
        </div>
    </header>

    <!-- HERO SECTION -->
    <section class="hero">
        <div class="container hero-content">
            <div class="hero-text fade-in">
                <h1>Better Ingredients.<br><span>Delivered Hot.</span></h1>
                <p>Fresh dough, real cheese, bold toppings, and deals made for every craving. Taste the difference today.</p>
                <div class="hero-btns">
                    <a href="#build" class="btn btn-primary">Order Now</a>
                    <a href="#deals" class="btn btn-outline">View Deals</a>
                </div>
            </div>
            <div class="hero-image fade-in">
                <img src="https://images.unsplash.com/photo-1513104890138-7c749659a591?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Delicious Hot Pizza" style="border-radius: 50%;">
            </div>
        </div>
    </section>

    <!-- DEALS SECTION -->
    <section id="deals">
        <div class="container">
            <h2 class="section-title fade-in">Exclusive Deals</h2>
            <div class="deals-grid">
                <div class="deal-card fade-in">
                    <span class="badge">Popular</span>
                    <img src="https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Large Combo">
                    <h3>Large Pizza Combo</h3>
                    <p>1 Large Pizza, 1 Side, 2 Drinks</p>
                    <div class="deal-price">$22.99</div>
                    <button class="btn btn-secondary">Claim Deal</button>
                </div>
                <div class="deal-card fade-in">
                    <span class="badge">Value</span>
                    <img src="https://images.unsplash.com/photo-1604381536136-a859e9c3e86c?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Family Meal">
                    <h3>Family Meal Deal</h3>
                    <p>2 Large Pizzas, 2 Sides, 2L Soda</p>
                    <div class="deal-price">$34.99</div>
                    <button class="btn btn-secondary">Claim Deal</button>
                </div>
                <div class="deal-card fade-in">
                    <span class="badge">Limited Time</span>
                    <img src="https://images.unsplash.com/photo-1590947132387-155cc02f3212?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Student Special">
                    <h3>Lunch Special</h3>
                    <p>1 Personal Pizza & Drink</p>
                    <div class="deal-price">$9.99</div>
                    <button class="btn btn-secondary">Claim Deal</button>
                </div>
            </div>
        </div>
    </section>

    <!-- MENU PREVIEW -->
    <section id="menu">
        <div class="container">
            <h2 class="section-title fade-in">Explore the Menu</h2>
            <div class="menu-cats fade-in">
                <div class="menu-cat active">Pizzas</div>
                <div class="menu-cat">Sides</div>
                <div class="menu-cat">Wings</div>
                <div class="menu-cat">Desserts</div>
                <div class="menu-cat">Drinks</div>
            </div>
            <div class="menu-grid">
                <div class="menu-item fade-in">
                    <img src="https://images.unsplash.com/photo-1574071318508-1cdbab80d002?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" alt="Pepperoni">
                    <h3>Classic Pepperoni</h3>
                    <p>$14.99</p>
                </div>
                <div class="menu-item fade-in">
                    <img src="https://images.unsplash.com/photo-1571997478779-2adcbbe9ab2f?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" alt="Margherita">
                    <h3>Margherita</h3>
                    <p>$13.99</p>
                </div>
                <div class="menu-item fade-in">
                    <img src="https://images.unsplash.com/photo-1513104890138-7c749659a591?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" alt="Supreme">
                    <h3>Meat Supreme</h3>
                    <p>$17.99</p>
                </div>
                <div class="menu-item fade-in">
                    <img src="https://images.unsplash.com/photo-1604381536136-a859e9c3e86c?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" alt="Veggie">
                    <h3>Veggie Delight</h3>
                    <p>$15.99</p>
                </div>
            </div>
            <div style="text-align: center; margin-top: 40px;" class="fade-in">
                <button class="btn btn-outline">View Full Menu</button>
            </div>
        </div>
    </section>

    <!-- BUILD YOUR PIZZA -->
    <section id="build">
        <div class="container">
            <h2 class="section-title fade-in">Craft Your Masterpiece</h2>
            <div class="build-section fade-in">
                <div class="build-options">
                    <h3>1. Choose Size</h3>
                    <div class="options-group">
                        <div class="opt-btn">Small</div>
                        <div class="opt-btn selected">Medium</div>
                        <div class="opt-btn">Large</div>
                    </div>
                    <h3>2. Choose Crust</h3>
                    <div class="options-group">
                        <div class="opt-btn selected">Original Pan</div>
                        <div class="opt-btn">Thin Crust</div>
                        <div class="opt-btn">Stuffed Crust</div>
                    </div>
                    <h3>3. Choose Toppings</h3>
                    <div class="options-group">
                        <div class="opt-btn selected">Pepperoni</div>
                        <div class="opt-btn">Mushrooms</div>
                        <div class="opt-btn selected">Extra Cheese</div>
                        <div class="opt-btn">Olives</div>
                        <div class="opt-btn">Onions</div>
                    </div>
                    <button class="btn btn-primary" style="margin-top: 20px;">Start Building</button>
                </div>
                <div class="build-preview">
                    <img src="https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Custom Pizza Preview" style="border-radius: 50%;">
                </div>
            </div>
        </div>
    </section>

    <!-- WHY CHOOSE US -->
    <section id="benefits">
        <div class="container">
            <div class="benefits-grid">
                <div class="benefit-card fade-in">
                    <div class="benefit-icon">🌾</div>
                    <h3>Fresh Dough</h3>
                    <p>Made in-house daily. Never frozen.</p>
                </div>
                <div class="benefit-card fade-in">
                    <div class="benefit-icon">🍅</div>
                    <h3>Quality Ingredients</h3>
                    <p>Locally sourced veggies and premium meats.</p>
                </div>
                <div class="benefit-card fade-in">
                    <div class="benefit-icon">🔥</div>
                    <h3>Hot Guarantee</h3>
                    <p>Delivered steaming hot or your next one is free.</p>
                </div>
                <div class="benefit-card fade-in">
                    <div class="benefit-icon">📱</div>
                    <h3>Easy Ordering</h3>
                    <p>Seamless online ordering in just 3 clicks.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- REWARDS -->
    <section id="rewards">
        <div class="container fade-in">
            <div class="rewards-section">
                <h2>Earn Points With Every Slice</h2>
                <p>Join rewards and get exclusive deals, free pizza, and birthday surprises.</p>
                <button class="btn" style="background:var(--white); color:var(--red);">Join Rewards Now</button>
            </div>
        </div>
    </section>

    <!-- REVIEWS -->
    <section id="reviews">
        <div class="container">
            <h2 class="section-title fade-in">Loved by Pizza Fans</h2>
            <div class="reviews-grid">
                <div class="review-card fade-in">
                    <div class="stars">★★★★★</div>
                    <p>"Best pizza in the city! The crust is perfectly crispy and they don't skimp on the cheese."</p>
                    <h4 style="margin-top: 15px;">- Sarah J.</h4>
                </div>
                <div class="review-card fade-in">
                    <div class="stars">★★★★★</div>
                    <p>"The app makes ordering so easy, and delivery is always lightning fast. Highly recommend the Meat Supreme!"</p>
                    <h4 style="margin-top: 15px;">- Mike T.</h4>
                </div>
                <div class="review-card fade-in">
                    <div class="stars">★★★★★</div>
                    <p>"Their rewards program actually gives good freebies. We order for family night every Friday!"</p>
                    <h4 style="margin-top: 15px;">- Emily R.</h4>
                </div>
            </div>
        </div>
    </section>

    <!-- STORE LOCATOR -->
    <section id="locator">
        <div class="container fade-in">
            <h2 class="section-title">Find Your Nearest Slice</h2>
            <div class="locator-container">
                <div style="display: flex; gap: 20px; font-weight: 600;">
                    <label><input type="radio" name="orderType" checked> Delivery</label>
                    <label><input type="radio" name="orderType"> Pickup</label>
                </div>
                <div class="search-bar">
                    <input type="text" placeholder="Enter your delivery address or zip code...">
                    <button class="btn btn-primary">Find Store</button>
                </div>
                <!-- Placeholder for map -->
                <div style="width: 100%; height: 300px; background: #E9ECEF; border-radius: 16px; display: flex; align-items: center; justify-content: center; color: #888;">
                    Interactive Map Placeholder
                </div>
            </div>
        </div>
    </section>

    <!-- APP PROMO -->
    <section id="app">
        <div class="container app-promo fade-in">
            <div class="app-text">
                <h2>Get Pizza Faster With Our App</h2>
                <ul>
                    <li>1-Tap Express Checkout</li>
                    <li>Exclusive App-Only Daily Deals</li>
                    <li>Live Real-Time Order Tracking</li>
                </ul>
                <div style="display: flex; gap: 15px;">
                    <button class="btn btn-secondary">App Store</button>
                    <button class="btn btn-secondary">Google Play</button>
                </div>
            </div>
            <div>
                <img src="https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?ixlib=rb-4.0.3&auto=format&fit=crop&w=400&q=80" alt="Mobile App Experience" class="app-mockup">
            </div>
        </div>
    </section>

    <!-- FINAL CTA -->
    <section class="final-cta fade-in">
        <div class="container">
            <h2>Hungry Now?</h2>
            <p style="font-size: 1.2rem; margin-bottom: 30px;">Your next amazing pizza is only a few clicks away.</p>
            <button class="btn btn-primary" style="font-size: 1.2rem; padding: 18px 40px;">Order Now</button>
        </div>
    </section>

    <!-- FOOTER -->
    <footer>
        <div class="container">
            <div class="footer-grid">
                <div class="footer-col">
                    <h4 style="font-size: 1.5rem; margin-bottom: 15px;">Slice<span style="color:var(--green)">It</span></h4>
                    <p>Better ingredients, better pizza, delivered fast.</p>
                </div>
                <div class="footer-col">
                    <h4>Menu</h4>
                    <ul>
                        <li><a href="#">Pizzas</a></li>
                        <li><a href="#">Sides</a></li>
                        <li><a href="#">Desserts</a></li>
                        <li><a href="#">Drinks</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h4>Company</h4>
                    <ul>
                        <li><a href="#">About Us</a></li>
                        <li><a href="#">Careers</a></li>
                        <li><a href="#">Rewards</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h4>Legal</h4>
                    <ul>
                        <li><a href="#">Privacy Policy</a></li>
                        <li><a href="#">Terms of Service</a></li>
                        <li><a href="#">Accessibility</a></li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                &copy; 2026 SliceIt Pizza. All rights reserved. 
            </div>
        </div>
    </footer>

    <!-- JAVASCRIPT FOR ANIMATIONS AND PARALLAX -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            
            // 1. Generate Parallax Background Elements
            const bgContainer = document.getElementById('parallax-bg');
            const types = ['tomato', 'leaf', 'cheese'];
            const elementCount = 20; // Number of floating items
            
            for(let i = 0; i < elementCount; i++) {
                const el = document.createElement('div');
                const type = types[Math.floor(Math.random() * types.length)];
                el.classList.add('float-el', type);
                
                // Random positioning and speeds
                const startX = Math.random() * 100; // vw
                const startY = Math.random() * 150; // vh (spread further down)
                const speed = (Math.random() * 0.4) + 0.1; // multiplier for scroll
                
                el.style.left = `${startX}vw`;
                el.style.top = `${startY}vh`;
                el.dataset.speed = speed;
                el.dataset.rotation = Math.random() * 360; // initial rotation
                
                bgContainer.appendChild(el);
            }

            // 2. Scroll Parallax Effect
            const floatingElements = document.querySelectorAll('.float-el');
            const header = document.getElementById('header');
            
            window.addEventListener('scroll', () => {
                let scrollY = window.scrollY;
                
                // Floating ingredients parallax
                floatingElements.forEach(el => {
                    let speed = parseFloat(el.dataset.speed);
                    let baseRotation = parseFloat(el.dataset.rotation);
                    // Translate Y upwards based on scroll, and slowly rotate
                    el.style.transform = `translateY(-${scrollY * speed}px) rotate(${baseRotation + (scrollY * 0.05)}deg)`;
                });

                // Header styling on scroll
                if(scrollY > 50) {
                    header.style.padding = '10px 0';
                    header.style.boxShadow = '0 5px 20px rgba(0,0,0,0.1)';
                } else {
                    header.style.padding = '15px 0';
                    header.style.boxShadow = '0 2px 15px rgba(0,0,0,0.05)';
                }
            });

            // 3. Intersection Observer for Fade-ins
            const observerOptions = {
                threshold: 0.1,
                rootMargin: "0px 0px -50px 0px"
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target); // Run once
                    }
                });
            }, observerOptions);

            document.querySelectorAll('.fade-in').forEach(el => {
                observer.observe(el);
            });
            
            // 4. Simple Interaction for Build Pizza Option Buttons
            const optGroups = document.querySelectorAll('.options-group');
            optGroups.forEach(group => {
                const btns = group.querySelectorAll('.opt-btn');
                btns.forEach(btn => {
                    btn.addEventListener('click', (e) => {
                        // If it's toppings, allow multiple, else single select
                        if(group.previousElementSibling.innerText.includes('Toppings')) {
                            btn.classList.toggle('selected');
                        } else {
                            btns.forEach(b => b.classList.remove('selected'));
                            btn.classList.add('selected');
                        }
                    });
                });
            });
        });
    </script>
</body>
</html>
